import React from "react";

import logo from "./../assets/logo.png";

function Header() {
  return (
    <header>
      <nav>
        <img src={logo} />
      </nav>
    </header>
  );
}

export default Header;
